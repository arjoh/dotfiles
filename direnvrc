# Usage: use ruby <version>
#
# Loads the specified ruby version into the environent
#
use_ruby() {
  rbenv local $1
  layout ruby
}

# find local exports
[ -f ~/.direnv.export ] && source ~/.direnv.export

