- defaults:
    link:
      create: true
      relink: true

- link:
    ~/.oh-my-zsh:
    ~/.oh-my-zsh-custom:
    ~/.zshrc:
    ~/.vim:
    ~/.vimrc:

- shell:
  - [git submodule update --init --recursive, Installing submodules]
  - [test -x $(which zsh) && chsh -s $(which zsh), Making zsh the default shell]

